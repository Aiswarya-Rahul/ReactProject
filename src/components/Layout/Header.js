import classes from "./Header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>
      <h1>Pets owned by different genders</h1>
    </header>
  );
};

export default Header;
